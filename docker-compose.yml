services:
  dashcraft:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashcraft-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
